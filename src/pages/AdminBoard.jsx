import { Typography } from '@mui/material';

const AdminBoard = () => {
  return (
    <Typography variant="h4" sx={{ p: 3 }}>
      Admin Dashboard
    </Typography>
  );
};

export default AdminBoard;