import { Typography } from '@mui/material';

const Home = () => {
  return (
    <Typography variant="h4" sx={{ p: 3 }}>
      Welcome to the Home Page!
    </Typography>
  );
};

export default Home;